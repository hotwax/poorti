<?xml version="1.0" encoding="UTF-8"?>
<services xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/service-definition-3.xsd">

    <!-- consume service to import data using OOTB EntityDataLoader
         from Moqui json data file content read from SFTP location -->
    <service verb="consume" noun="DataImportSystemMessage">
        <implements service="org.moqui.impl.SystemMessageServices.consume#SystemMessage"/>
        <actions>
            <entity-find-one entity-name="moqui.service.message.SystemMessage" value-field="systemMessage"/>
            <script><![CDATA[
                import groovy.json.JsonSlurper;
                if (dataString) {
                   jsonObj = new JsonSlurper().parseText(systemMessage.messageText)
                    if (jsonObj instanceof List && ((List) jsonObj).size() > 0) {
                        for (Object obj in (List) jsonObj) {
            ]]></script>
            <service-call name="co.hotwax.fulfillment.FufillmentServices.import#FulfillmentOrder" async="false"
                          in-map="obj" out-map="orderOut"/>
            <script> 
                        }
                    }
                }
            </script>
        </actions>
    </service>
</services>
